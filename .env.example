# Flux Replicate MCP Server Configuration

# Required: Your Replicate API token
REPLICATE_API_TOKEN=r8_your_token_here

# Optional: Default Flux model to use
FLUX_DEFAULT_MODEL=flux-pro

# Optional: Default output format for images
FLUX_OUTPUT_FORMAT=jpg

# Optional: Default quality for lossy formats (1-100)
FLUX_OUTPUT_QUALITY=80

# Optional: Custom working directory for generated images
# If not set, uses platform-specific defaults:
# - Windows: %USERPROFILE%\Documents\FluxImages
# - macOS: ~/Pictures/FluxImages
# - Linux: ~/Pictures/FluxImages (fallback: ~/flux-images)
# - Other: ~/flux-images
# FLUX_WORKING_DIRECTORY=/custom/path/to/images
